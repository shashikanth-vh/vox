apiVersion: v1
kind: Service
metadata:
  name: {{ include "access.fullname" . }}
  labels:
    {{- include "access.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "access.selectorLabels" . | nindent 4 }}
