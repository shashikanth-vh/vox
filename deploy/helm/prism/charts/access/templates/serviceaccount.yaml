{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "access.serviceAccountName" . }}
  labels:
    {{- include "access.labels" . | nindent 4 }}
{{- end }}
