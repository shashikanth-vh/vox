{{- if not .Values.register.apiKey.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pulse.secretName" . }}
  labels:
    {{- include "pulse.labels" . | nindent 4 }}
type: Opaque
stringData:
  register-api-key: {{ .Values.register.apiKey.value | quote }}
{{- end }}
