apiVersion: v1
kind: Service
metadata:
  name: {{ include "vocx.fullname" . }}
  labels:
    {{- include "vocx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vocx.selectorLabels" . | nindent 4 }}
