{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vocx.serviceAccountName" . }}
  labels:
    {{- include "vocx.labels" . | nindent 4 }}
{{- end }}
