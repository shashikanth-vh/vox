{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "stt.serviceAccountName" . }}
  labels:
    {{- include "stt.labels" . | nindent 4 }}
{{- end }}
