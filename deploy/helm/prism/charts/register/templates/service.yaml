apiVersion: v1
kind: Service
metadata:
  name: {{ include "register.fullname" . }}
  labels:
    {{- include "register.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "register.selectorLabels" . | nindent 4 }}
